export function SectionLabel({ children }: { children: React.ReactNode }) {
  return (
    <h2 className="mb-2.5 font-mono text-[10px] font-medium uppercase tracking-[0.16em] text-ink-3">{children}</h2>
  );
}
